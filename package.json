{
  "name": "webrtc-calling-app",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist && cp -r public/* dist/ && echo '/* /index.html 200' > dist/_redirects"
  },
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.3.1",
    "uuid": "^8.3.2"
  }
}